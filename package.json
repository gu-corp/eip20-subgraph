{
  "name": "@gu-corp/erc20-subgraph",
  "version": "1.0.1-build.6",
  "description": "Subgraph for indexing all ERC20 token balances and approvals",
  "scripts": {
    "clean": "rimraf build generated",
    "auth": "graph auth",
    "remove": "graph remove",
    "create": "graph create",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "graph": "graph",
    "gen:yaml:cmc": "node ./scripts/gen-yaml-from-cmc.js",
    "gen:yaml:cmc:mainnet": "node ./scripts/gen-yaml-from-cmc.js --network mainnet",
    "gen:yaml:tokenlist": "node ./scripts/gen-yaml-from-tokenlist.js",
    "gen:yaml:tokenlist:mainnet": "node ./scripts/gen-yaml-from-tokenlist.js --network mainnet",
    "gen:yaml:tokenlist:sepolia": "node ./scripts/gen-yaml-from-tokenlist.js --network sepolia"
  },
  "license": "MIT",
  "dependencies": {
    "@amxx/graphprotocol-utils": "^1.1.0",
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.96.0",
    "@graphprotocol/graph-ts": "^0.38.1",
    "rimraf": "^6.0.1"
  }
}
